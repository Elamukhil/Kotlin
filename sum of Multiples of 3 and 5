fun sum():Int
{
    var x=0
    (1..999).forEach{
        if(it%3==0 || it%5==0)
        {
            x+=it
        }
    }
    return x
}
fun main(args: Array<String>) {
	println(sum())
}

//233168
